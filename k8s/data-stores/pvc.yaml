apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: consumer-output-pvc
  namespace: consumer-pipeline
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
